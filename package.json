{
  "name": "test-ueat",
  "version": "1.0.0",
  "description": "create a function that converts a roman number string to an integer",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dennis Huallanca Zavaleta",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}